# Hooks

This directory contains custom React hooks that encapsulate shared business logic and side effects, such as hash-based routing, PWA installation lifecycle, and localStorage persistence for bookmarks and history.

For more details on how these hooks drive the application state, see the [Project Map](../../AGENTS.md#project-map).
