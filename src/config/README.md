# Config

This directory contains centralized configuration files and registries for the application. For example, `sections.js` defines the modules available on the Home Hub and their visual styles.

For more details on how configuration is used, see the [Project Map](../../AGENTS.md#project-map).
